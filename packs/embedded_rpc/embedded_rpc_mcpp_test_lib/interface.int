interface PWM
  # Events
  EVENT PwmUpdate(uint8_t channel, uint16_t value)

  # Functions
  void SetPwm(uint8_t channel, uint16_t value) # second
  uint16_t GetPwm(uint8_t channel)
  void ResetPwm()
end

data RCArray
  uint8_t data[10]
end

interface Foo
  void DoThat()
  void DoThis(RCArray rcarray)
  uint32_t Post(uint32_t p)
end

enum Scope
    a
    b = 20
    c
end

data KeyId
  uint32_t id
  uint16_t key[10]
  Scope scope
end

data Keys
  KeyId keys[2]
end

interface Bar
    KeyId GetKeyId()
    void SetKeyId(KeyId kid)

    bool IO_In(uint16_t<In> v)
    bool IO_Out(uint16_t<Out> v)
    void IO_InOut(uint16_t<InOut> v)
end