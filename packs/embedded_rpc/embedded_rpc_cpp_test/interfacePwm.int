enum Channel
  Low
  High
end

interface PWM
  # Events
  EVENT PwmUpdate(Channel channel, uint16_t value)

  # Functions
  void SetPwm(Channel channel, uint16_t value) = 100 # second
  uint16_t GetPwm(Channel channel)
  void ResetPwm()

  void SetName(PCString name)
  PCString GetName()
end