import Array
import PCString

c_alias Array array_t

interface Foo
  EVENT UpdateMe(uint16_t v)
  EVENT UpdateMeArray(Array a)

  void DoThis(uint16_t v)
  uint16_t DoThat(uint8_t me)
  void DoThisArray(Array a)
  PCString UpperCase(PCString str)
end


enum Scope
 Small
 Normal = 15
 Big
end

data KeyId
  uint32_t id
  uint16_t key[10]
  Array    a[2]
  Scope scope
end

c_alias KeyId keyId_t
c_alias Scope Scope_t

interface Bar
    KeyId GetKeyId()
    void SetKeyId(KeyId kid)

    bool IO_In(uint16_t<In> v)
    bool IO_Out(uint16_t<Out> v)
    void IO_InOut(uint16_t<InOut> v)
end